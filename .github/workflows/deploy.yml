name: Deploy to Cloudflare

# Cloudflare deployment is now manual-only. The app is self-hosted as a Docker
# image on the k3s cluster (see build-ghcr.yml and deploy/k8s/); pushes no
# longer deploy to Cloudflare. Run this from the Actions tab if ever needed.
on:
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Provision + deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          ACCESS_PASSCODE: ${{ secrets.ACCESS_PASSCODE }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
          WRANGLER_SEND_METRICS: "false"
        run: bash scripts/deploy.sh
