apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: epub-reader-data
  namespace: default
spec:
  accessModes: ["ReadWriteOnce"]
  # ~80 MB per 3000-chapter book (raw .epub + parsed content).
  resources: {requests: {storage: 10Gi}}
